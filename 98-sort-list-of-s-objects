public List<Account> getAccounts(List<Account> accounts) {
    // code here
    if(accounts.size() > 0){
       accounts.sort((a, b) => b.AnnualRevenue.compareTo(a.AnnualRevenue));
    }
    return accounts;
}
// write extra class here 
