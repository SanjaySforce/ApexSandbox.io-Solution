public String divide(Integer a, Integer b){
    //code here
    String result = '';
    if(b == 0){
        result = 'Divide by 0';
    }else{
        result = string.ValueOf(Math.mod(a,b));
    }
    return result;
}