public Decimal average(List<Decimal> numbers){
    // your code here
    Decimal avg = 0;
    if(numbers.size() > 0){
        Decimal total = 0;
        for(Decimal i: numbers){
            total += i;
        }
        avg = total/numbers.size();
        avg = avg.setScale(2);
    }
    return avg;
}